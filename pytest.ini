[pytest]
asyncio_mode = auto
testpaths = tests
# The homeassistant pytest plugin (from pytest_homeassistant_custom_component,
# installed in the shared integration venv) imports fcntl at collection time and
# crashes on Windows. This library has no HA dependency, so disable that plugin.
addopts = -p no:homeassistant
